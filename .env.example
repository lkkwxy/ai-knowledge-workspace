# 复制为 .env.local 并填入真实 key（.env.local 已被 .gitignore 屏蔽，绝不提交）
# 主模型二选一，前期固定一个把链路跑通

# Claude
#ANTHROPIC_API_KEY=

# 或 OpenAI
# OPENAI_API_KEY=

DEEPSEEK_API_KEY=

# Supabase Postgres 连接串（Session pooler / IPv4）；从 Supabase → Connect → Session pooler 复制，密码里特殊字符需 URL 编码
DATABASE_URL=