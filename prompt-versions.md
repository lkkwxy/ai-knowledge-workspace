# Prompt Versions · Prompt 版本变化

> 四件套之一(见 `../roadmap.md` 第十节)。每次改 system / 关键 prompt 就追加一版,最新在最上。
> 模板:`## vN` — 日期、所在文件、改动内容、改动原因、改后 eval 结果(哪些 case 受影响)。

---

## extract-v2 — 收紧待办/标签的任务边界

- **日期**:2026-06-04
- **所在文件**:`lib/prompts.ts`(`prompts.extract`)
- **内容**:
  ```
  从下面这段文本里提取 标题、摘要、标签、待办。规则：
  - 待办(todos)：只收需要主动去做的具体动作。一件事只写一条、不要拆成多条，并保留其中的时间/截止信息(如「下周一上午前」)。明确表示推迟、暂缓、放弃、或已完成的，都不算待办、不要收。没有要做的事就返回空数组。
  - 标签(tags)：只给内容主题词，不要「待办/总结/笔记」这类描述文档本身的元词。
  - 标题、摘要：各一句，概括主旨。

  文本：
  ${text}
  ```
- **改动原因**:eval Case 015(混乱输入)在 v1 下暴露**语义层**失败,且多跑不稳——一次把「retention 先放放」(决定推迟)当待办 + tags 混入 meta 词「待办事项」;另一次把单个任务「下周一前拉数据」拆成 2 条并丢了截止时间(见 failure-cases Bad Case 3)。schema 没崩,崩的是「字段内容对不对」。v1 prompt 太简(只说「提取…待办事项」),没界定任务边界,故在 prompt 里补三条规则(待办:主动动作/一事一条/留时间/推迟不算;tags:只给主题词)。schema 不动——结构归 schema,任务规则归 prompt。
- **改后 eval 结果**:✅ Case 015 连跑 3 次**稳定转 pass**(todos 恒为 1 条带截止时间、retention 排除、tags 无 meta 词)。回归 006/007/008/009/011 **无误伤**:009 五条不同待办未被「一事一条」误并、011 三条埋散文待办仍全抽、007/008 空待办仍空。extract 整组 10/10。

## extract-v1 — 信息提取模板(初版)

- **日期**:2026-06-03
- **所在文件**:`lib/prompts.ts`(`prompts.extract`),由 `lib/extract.ts` 调用
- **内容**:
  ```
  从下面这段文本里提取标题、摘要、标签和待办事项。

  文本：
  ${text}
  ```
- **改动原因**:W2「Prompt 模板目录」首版。设计上**故意只描述任务、不在 prompt 里求 JSON**——输出结构交给 Zod schema(`Output.object`)锁,prompt 保持干净(对比 extract-naive 的「嘴上求 JSON」)。同目录还放了 `summarize`/`todos` 两个模板,当前未接入(由 extract 字段覆盖)。
- **改后 eval 结果**:006~015 共 10 条实跑,**9 过 1 不过**。唯一 false=Case 015:混乱输入下 schema 没崩(格式层稳),但把「retention 先放放」误当待办、tags 混入 meta 词(语义层崩,见 failure-cases Bad Case 3)→ 这正是 extract-v1 任务定义不够细、待 v2 收紧的靶子。观察项:008 tags 数软约束、012 跨语言。详见 `eval-cases.json`。

> 注:下面 v2 / v1 是 W1 chat 的 system prompt 版本,与 extract 模板互不影响,分开计数。

## v2 — 强化中文约束

- **日期**:2026-05-31
- **所在文件**:`week-01/chat/app/api/chat/route.ts`
- **内容**:
  ```
  你是一个简洁、诚实的助手。无论用户用什么语言提问，都始终用中文回答。
  ```
- **改动原因**:eval Case001 暴露漏洞——英文提问(What is the capital of Japan?)时模型跟着用英文回答,违反 v1 的「用中文回答」。原措辞语气太弱,被输入语言带跑。
- **改后 eval 结果**:✅ 已重跑(各用例独立新对话):**5/5 通过**。Case001 由英文转中文(「日本的首都是东京」),其余 4 条保持 pass,无回归。偶见「的的」叠字小瑕疵,不影响判定。

## v1 — 初版 system prompt

- **日期**:2026-05-31
- **所在文件**:`week-01/chat/app/api/chat/route.ts`
- **内容**:
  ```
  你是一个简洁、诚实的助手，用中文回答。
  ```
- **改动原因**:跑通链路的第一版,先定个最小基线。
- **改后 eval 结果**:eval-cases 尚为空,未跑回归。
